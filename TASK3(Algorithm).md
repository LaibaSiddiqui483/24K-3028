Ask the user to enter Num1
Ask the user to enter Num2
SET the operator to +,-,*,/,%
SET answer=0
IF the operator is "+" THEN , DISPLAY "answer=Num1+Num2"
IF the operator is "-" THEN , DISPLAY "answer=Num1-Num2"
IF the operator is "*" THEN, DISPLAY "answer=Num1*Num2"
IF the operator is "/" THEN, DISPLAY "answer=Num1/Num2"
IF the operator is "%" THEN, DISPLAY "answer=Num1%Num2"
